"use client"

// ManagerReportView is a role-based sub-view rendered inside the main report page
// for users with the 'manager' role. It shares the same interface as the My Reports page.
// This component re-uses the same logic but omits the standalone page wrapper.

export { default } from '../my_report/page';